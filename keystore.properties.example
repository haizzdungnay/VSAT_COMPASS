# Copy to keystore.properties (gitignored) for release builds.
# Generate keystore: keytool -genkey -v -keystore vsat-release.keystore -alias vsat -keyalg RSA -keysize 2048 -validity 10000
storeFile=../vsat-release.keystore
storePassword=
keyAlias=vsat
keyPassword=

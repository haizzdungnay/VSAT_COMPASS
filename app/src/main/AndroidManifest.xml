<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".VsatApp"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VSAT_COMPASS">

        <!-- ── Core ── -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.auth.LoginActivity"    android:exported="false" />
        <activity android:name=".ui.auth.RegisterActivity" android:exported="false" />
        <activity android:name=".MainActivity"             android:exported="false" />

        <!-- ── Exam (Student) ── -->
        <activity android:name=".ui.exam.ExamDetailActivity"           android:exported="false" />
        <activity android:name=".ui.exam.session.ExamSessionActivity"  android:exported="false" />
        <activity android:name=".ui.exam.session.ExamResultActivity"   android:exported="false" />

        <!-- ── Admin Mode ── -->
        <activity
            android:name=".ui.admin.AdminActivity"
            android:exported="false"
            android:label="Quản trị V-SAT" />

        <!-- Admin: Question management -->
        <activity android:name=".ui.admin.questions.AdminReviewQuestionActivity" android:exported="false" />

        <!-- Admin: Exam management -->
        <activity android:name=".ui.admin.exam.AdminCreateExamActivity" android:exported="false" />

        <!-- ── Collaborator ── -->
        <activity android:name=".ui.collaborator.CollaboratorWorkspaceActivity" android:exported="false" />
        <activity android:name=".ui.collaborator.QuestionEditorActivity"        android:exported="false" />

    </application>

</manifest>
